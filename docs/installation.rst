============
Installation
============

At the command line::

    $ easy_install pyrecommend

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyrecommend
    $ pip install pyrecommend
